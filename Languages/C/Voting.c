#include<stdio.h>
int main()
{
	char name[] = "Jaidhev.A";
	int age;
	printf("Enter your age: ");
	scanf("%d", age);
	if(age >= 18)
		printf("You are eligible to vote.");
	else if(age < 18)
	{
		printf("You are not eligible to vote.");
		printf("You are eligible for voting after %d ")
	}
		
		
}
